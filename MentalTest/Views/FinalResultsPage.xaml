<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MentalTest.Views.FinalResultsPage"
             Title="Test Results"
             NavigationPage.HasNavigationBar="False">

    <ContentPage.Content>
        <StackLayout VerticalOptions="Center" HorizontalOptions="Center" Padding="20">

            <Label Text="Your Test Results" FontSize="Large" HorizontalOptions="Center"/>

            <Label Text="{Binding FinalResultMessage}" FontSize="Medium" HorizontalOptions="Center" Margin="0,20,0,0"/>

            <Button Text="Return to Home" Command="{Binding ReturnHomeCommand}" Margin="0,40,0,0"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
